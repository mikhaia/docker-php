<div class="form-group">
    <label for="password-input-{{ $name }}">{{ $name }}</label>
    <input type="password"
        name="{{ $name }}" 
        id="password-input-{{ $name }}"
        class="form-control"
        placeholder="{{ $name }}" >
</div>
